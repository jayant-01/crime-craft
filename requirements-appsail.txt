# Lean dependency set for the AppSail production deploy.
#
# The full requirements.txt pulls heavy ML libs (torch via sentence-transformers,
# xgboost, shap, scikit-learn, numpy) that are ONLY needed for live RAG / model
# training. They are lazy-imported, so the app runs fine without them in the
# default stub mode (templated RAG answers + citations, recidivism stub).
#
# Point the AppSail "build file" at THIS file for a fast, reliable first deploy.
# Add the ML libs (or move to Catalyst QuickML / Zia) when you switch RAG to live.

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.6.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
python-dotenv==1.0.1
reportlab==4.2.5

# Zoho Catalyst SDK — required in production (CATALYST_ENABLED=true)
zcatalyst-sdk-python==1.2.0
