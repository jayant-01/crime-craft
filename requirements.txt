fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
email-validator==2.2.0   # required by models.User.email (pydantic EmailStr)
pydantic-settings==2.6.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
python-dotenv==1.0.1

# Zoho Catalyst
# zcatalyst-sdk-python==1.2.0

# RAG (only needed when RAG_PROVIDER=live; safe to install for dev too)
anthropic==0.40.0
qdrant-client==1.12.1
sentence-transformers==3.2.1

# PDF export
reportlab==4.2.5

# Predictive analytics (recidivism scoring)
xgboost==2.1.2
shap==0.46.0
scikit-learn==1.5.2
numpy==2.1.3

# Voice (lazy-imported — only needed when voice endpoints are used)
# openai-whisper==20240930  # uncomment to enable real STT (large download)

# Dev / tests
pytest==8.3.3
httpx==0.27.2
