# Lean runtime dependencies — this IS the AppSail build file (the Python stack
# expects a file named exactly `requirements.txt`). The app runs fully in its
# default stub mode with just these, and the test suite needs nothing more.
#
# Heavy ML libraries for LIVE features (RAG over Qdrant + hosted LLM, recidivism
# model training) are lazy-imported and live in requirements-ml.txt — install
# those only when you switch RAG/recidivism to live.

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
email-validator==2.2.0            # models.User.email (pydantic EmailStr)
pydantic-settings==2.6.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
python-dotenv==1.0.1
reportlab==4.2.5                   # PDF export

# Zoho Catalyst SDK — REQUIRED in production (CATALYST_ENABLED=true)
zcatalyst-sdk-python==1.2.0

# Dev / tests
pytest==8.3.3
httpx==0.27.2
