services:
  qdrant:
    image: qdrant/qdrant:v1.12.1
    container_name: crime-craft-qdrant
    ports:
      - "6333:6333"  # REST
      - "6334:6334"  # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
